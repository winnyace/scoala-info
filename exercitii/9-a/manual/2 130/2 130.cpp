#include <iostream>

using namespace std;

int main()
{
    // 2/130 sa se calculeze lungimea (2*r) si aria (pi*r*r)al unui cerc cu r dat de la tastatura.
    int r;
    float a, l, PI = 3.14;
    cout << "r = "; cin >> r;
    a = PI*(rcr*r);
    l = 2*r;
    cout << endl << a;
    cout << endl << l;
    return 0;
}
